преобразование строк

ReadString('\n')
TrimSpace(input)
NewReplacer("#", "o")
Join возвращает строку соединеную через разделитель, принимает сегмент
Join(phrases[:len(phrases)-1], ", ") разделяет кроме последней
Join([]string{"05", "14", "2018"}, "/"))
